# 克拉拉新年音乐会 - 活动注意事项测评系统

这是一个基于 **Vue 3 + TypeScript + Vite** 开发的在线测评 Web 应用，旨在帮助参与“克拉拉新年音乐会”的演职人员和观众快速掌握活动注意事项。

## 📸 项目预览

该应用包含以下核心功能页面：

1.  **首页**：活动主题展示与开始测评入口。
2.  **答题页**：沉浸式答题体验，支持单选、判断、多选三种题型，实时显示答题进度。
3.  **结果页**：答题结束后自动评分，直观展示成绩。
4.  **解析页**：详细的试题解析，标记正确与错误选项，帮助用户查漏补缺。

## 🛠️ 技术栈

- **前端框架**: Vue 3 (Script Setup)
- **语言**: TypeScript
- **构建工具**: Vite
- **样式**: CSS3 (Flexbox/Grid, 响应式设计)
- **部署**: GitHub Actions (自动部署至 GitHub Pages)

## 🚀 快速开始

### 环境要求

- Node.js >= 18
- npm 或 pnpm

### 安装依赖

```bash
npm install
# 或者
pnpm install
```

### 本地开发

启动本地开发服务器，实时预览修改：

```bash
npm run dev
```

### 构建生产版本

构建用于生产环境的静态文件（输出到 `dist` 目录）：

```bash
npm run build
```

## 📂 项目结构

```
clara-test/
├── docs/                # 文档资料（含题库原始 Markdown）
├── src/
│   ├── assets/          # 静态资源
│   ├── data/            # 数据文件（题目配置 questions.ts）
│   ├── App.vue          # 主应用组件（包含所有核心逻辑与视图）
│   └── main.ts          # 入口文件
├── .github/workflows/   # GitHub Actions 自动部署配置
├── index.html           # HTML 模板
├── vite.config.ts       # Vite 配置文件
└── package.json         # 项目依赖配置
```

## 📝 题库说明

题目涵盖以下维度，共 20 题，满分 100 分：

- **单选题 (40分)**：到场时间、座位安排、流程时间点等。
- **判断题 (20分)**：行为规范细节判断。
- **多选题 (40分)**：禁忌行为、结束安排、钢琴保护细则等。

## 🚢 部署说明

本项目已配置 **GitHub Actions** 自动化部署。
每当代码推送到 `main` 分支时，会自动构建并发布到 GitHub Pages。

**手动配置步骤**：

1.  进入 GitHub 仓库 Settings -> Pages。
2.  在 "Build and deployment" 中，Source 选择 **GitHub Actions**。
